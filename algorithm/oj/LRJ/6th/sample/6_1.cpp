#include <queue>

using namespace std;

const int maxn = 1000;

deque<int> readyQ;
deque<int> blockQ;
int n, c[5], quantum;
int var[26];
int ip[maxn];
bool locked;
char prog[maxn][10];

int main(){
    int T;
    cin >> T;
    while(T--){
        cin >> n >> c[0] >> c[1] >> c[2] >> c[3] >> c[4] >> quantum;

    }
    return 0;
}
